<h1>This is <%= @needy.first_name %>'s Profile</h1>
<h2>Bio</h2>
<p class="about"><%= @needy.bio %></p>
<h3>This person is in need of:</h3>
<h3>Use colleciton_check_boxes?</h3>
<p>f.collection_check_boxes :category_ids, Category.all, :id, :name but use model method to filter </p>
<ul>

  <% if @items.empty? %>
    <li>Nothing at this time</li>
  <% else %>
    <%= form_with url: donator_donate_items_path do |form| %>
      <ul>
        <% @items.each_with_index do |item, index| %>
          <li>
            <%= fields_for "items[]", item do |f| %>
              <%= f.check_box :donator_id %>
              <%= f.label item["name"] %> - quantity: <%= f.label item["quantity"] %>
            <% end %>
          </li>
        <% end %>
      </ul>
      <%= form.submit "Donate Items" %>
    <% end %>

    <%= form_with url: donator_donate_items_path, local: true do |form| %>
      <% @items.each do |item| %>
        <li><input type="checkbox" name="[donator][donate_items][<%= item.id %>][]" value="checked" ><%= item.name %> - quantity: <%= item.quantity %></input></li>
      <% end %>
      <p class="callout">Note: You are only able to donate a <strong>total</strong> of 5 items per Month.</p>
      <%= form.submit "Donate Items" %>
    <% end %>
  <% end %>
</ul>
<ul>
  <%= form_with model: @needy, local: true do |form| %>
    <%= hidden_field :needy_id, value: @needy.id %>
    <%= form.collection_check_boxes :items, Item.all, :id, :name do |cb| %>
      <% cb.label do %>
        <li><%= cb.check_box + cb.text %></li>
      <% end %>
    <% end %>
    <%= form.submit %>
  <% end %>
</ul>
<%= link_to "Back to Your Profile", donator_path(@donator) %>
